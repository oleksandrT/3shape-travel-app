'use strict';

import { h, Component } from 'preact'
import 'preact/devtools'

export default class KievComponent extends Component {
  render (props, state) {
    return (
        <div>
            <h1>
                Kiev
            </h1>
        </div>
    )
  }
}
