'use strict';

import { h, Component } from 'preact'
import 'preact/devtools'

export default class CopenhagenComponent extends Component {
  render (props, state) {
    return (
        <div>
            <h1>
                Copenhagen
            </h1>
        </div>
    )
  }
}
